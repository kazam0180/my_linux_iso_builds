#!/bin/bash
set -e

useradd -m -s /bin/zsh -G sudo,video,audio user
echo "user:live" | chpasswd
echo "user ALL=(ALL) NOPASSWD:ALL" >> /etc/sudoers

chown root:root /etc/netplan/01-network-manager-all-eth.yaml
chmod 600 /etc/netplan/01-network-manager-all-eth.yaml

systemctl enable NetworkManager
